on pageload body.innerHTML = html code als string 
    - load currentweather icon and write in current weather section
        dom element = currentWeather.getIconLink()

    - load current temperature in either celsius or fahrenheit and write in
      current weather section
        dom Element = kelvinToCelsius(currentWeather.temperature);

    - load currentweathers pop and write in current weather section
        dom Element = currentWeather.precipitation;

    - load windspeed current and write in current weather section
        dom Element = currentWeather.windspeed;

    - load humidity and...
        dom Element = currentWeather.humidity;

    - load currentWeather Day in writen format
        dom Element = currentWeather.getWeekDay();

    - load current date in format Monday, March 29 or 03.December 
      depending on language
        dom Element = currentWeather.getMonthAndDayDate();

    -load and write City Name, CountryCode
        dom Element = currentWeather.getCityNameAndCountryCode();

    - load Current Time
        dom Element = currentWeather.getCurrentTime();

    - create number of hour elements depending on how many hour data the day similar
      to the current day has left + the current weather if 
      currentWeather.getCurrentTime !== dayObjects[0].data[0].time
        - first element :
            domTime = currentWeather.getCurrentTime();
            domIcon = currentWeather.getIconLink();
            domTemp = currentWeather.temperature;

        - next Elements if currentWeather.getWeekDay() === dayObjects[0].getWeekDay(): 
            loop over first data array of the first object in the days array
            for(let i = 0; i < dayObjects([0].data.length; i += 1) {
                domTIme = dayObjects[0].data[i].time;
                domIcon = dayObjects[0].data[i].getIconLink();
                domTemp = dayObjects[0].data[i].temperature;
            } 

            else if currentWeather.getWeekDay() !== dayObjects[0].getWeekDay()
                no new elements

    - create day Cards, number representing the length of dayObjects array plus one
      if currentWeather.getWeekDay() !== dayObjects[0].getWeekDay()
        - first day Card :
            domWeekday = "Today";
            domMonthandData = currentWeather.getMonthAndDayDate();
            domIcon = currentWeather.getIconLink();
            domTempScale = currentWeather.temperature;
        
        - next day cards : 
            for (let i = 0; i < dayObjects.length;i += 1) {
                domWeekday = dayObjects[i].getWeekDay();
                domMonthandData = dayObjects[i].getMonthAndDayDate();
                domIcon = dayObjects[i].getIconLink();
                domMinTemp = dayObjects[i].getMinTemp();
                domMaxTemp = dayObjects[i].getMaxTemp();
            }

        